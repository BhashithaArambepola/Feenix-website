---
deployment:
  tasks:
    # Repository is already located at /home/feenufzd/public_html.
    # Git pull updates files in place, so deployment only records success.
    - /bin/echo "Deployment triggered for Feenix-website at $(date)"
    # If you later move the repo outside public_html, use rsync instead:
    # - export DEPLOYPATH=/home/feenufzd/public_html
    # - /bin/rsync -az --delete --exclude='.git*' --exclude='node_modules' ./ "$DEPLOYPATH/"
